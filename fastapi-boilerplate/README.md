# FastAPI boilerplate

Boilerplate project with some dependency injection for database interactions.

## Setting up

```sh
git clone git@github.com:pontuscode/fastapi_boilerplate.git
cd fastapi_boilerplate.git
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
pytest tests
```
